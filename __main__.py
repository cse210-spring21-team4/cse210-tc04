# TODO: Add entry point code here

""" (AH reminder)
Define a new class called Dealer.
    Add a class comment that briefly identifies,
    a) what a Dealer is, b) the responsibility of a Dealer,
    and c) the name and type of its attributes.

Create an empty method stub for each of the Dealer behaviors.
    Add a method comment that briefly describes,
    a) what the method does,
    b) the name and type of the expected arguments,
    and c) the type of the return value (if any).
"""


# (AH) import from Director class.
from game.director import Director

# (AH) director variable instantiates an instance of the Director class.
director = Director()
director.start_game()

